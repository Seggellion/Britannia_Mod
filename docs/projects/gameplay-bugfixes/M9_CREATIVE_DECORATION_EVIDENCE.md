# M9 Creative decoration evidence

Automated gate passed: `tmp/gameplay-bugfixes/m9-2.log`, 1,154 required GameTests, 2.544 minutes; seven focused JUnit tests, zero failures/errors/skips. Command: Java21, `gradlew.bat test --tests '*StalactiteResourceParityTest' --tests '*HedgeBushCollisionTest' --tests '*DecorativeMultiblockBlockTest' runGameTestServer -x processResources --no-configuration-cache --console=plain`. Resources were unchanged from the normal M8 build; final packaging runs the normal resource path.

Creative bypasses the shared decorative item's base sturdy substrate check. Crate foundation rules still run through their own override; Adventure scarecrows retain community-only occupied-cell permission. All structure cells validate bounds, border, loaded space, replaceability, occupied block entities, permissions and entity collision before writing. Unsupported ordinary-mode placement keeps its cost/refusal behavior.

Fern, hedge, blood, stalactite and the three-part iron fence gate persist `creative_origin`; each constructor explicitly defaults it to false. Missing old-save properties decode to false. Neighbor updates retain Creative substrates, while missing door structural parts still dismantle the assembly. Generic multiblock decorations and wine bottles have no recurring substrate check and receive no persistent flag. Bottle contents and exact components survive BE serialization. A stalactite remains one logical anchor, with its occupied lower cell checked. The dedicated triple-door item validates the full three-cell footprint before writing and rolls back refused writes before consumption.

Seven new registered server tests cover the recurring families, Creative item cost, legacy defaults, support removal, exact blockstate serialization, door teardown, all seven requested scarecrow surfaces and all four cells, occupied door/stalactite block entities, bottle component/NBT conservation, supported Survival costs/support loss and the Adventure community-only boundary. Existing crate, stall, Grabby, hedge, stalactite and door regressions passed with the full registered suite.

The first run had 36 failures. It exposed the bypass incorrectly skipping crate subclass foundation rules and BooleanProperty's true enumeration default; both implementation defects were fixed. Historical crate/stall players were inside the placement geometry, and some mode-sensitive mocks always returned Creative despite their ability flags. Those fixtures now stand outside the geometry or use real named players. The production collision check was retained. Earlier failures remain in `m9-1.log` and the scratchpad.

Blockstate/BE serialization is not a client observation or actual chunk unload/server restart. Named M11 checks remain: place every family in the isolated client, remove substrates, cross a chunk-load boundary and restart; inspect each family in all three modes and verify protected actions/world limits/invalid geometry without item loss. No live world was modified. Existing valid saves need no migration. A downgrade ignores the new property and reinstates old substrate survival; unsupported Creative decorations may then break on updates. Preserve world backups for any separately authorized release.
