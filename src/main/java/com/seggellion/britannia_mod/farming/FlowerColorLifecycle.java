package com.seggellion.britannia_mod.farming;

/** Invalid legacy/config values remain representable so validation can reject them clearly. */
public enum FlowerColorLifecycle {
    SELECT_ON_SERVER_PLANTING_AND_STORE,
    SELECT_DURING_DESERIALIZATION
}
